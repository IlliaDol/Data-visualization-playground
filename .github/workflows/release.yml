name: Create Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build application
        run: npm run build
        
      - name: Create Release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false
          body: |
            ## 🚀 New Features
            
            ### AI-Powered Analysis
            - Intelligent chart recommendations
            - Automatic field detection
            - Statistical insights
            - Pattern recognition
            - Natural language queries
            
            ### Universal File Format Support
            Support for 20+ file formats:
            - CSV, TSV, Excel, JSON, XML
            - YAML, TOML, LOG files
            - Parquet, NumPy, Pickle
            - HDF5, Feather, Arrow
            - Avro, ORC, compressed formats
            
            ### Enhanced UI/UX
            - Modern design with AI indicators
            - Real-time preview
            - Dark/light mode
            - Responsive design
            
            ## 🔧 Technical Improvements
            - Universal file parser
            - Format detection
            - Data validation
            - Performance optimizations
            
            ## 📊 Chart Types
            16+ chart types including:
            - Bar, Line, Scatter, Pie
            - Histogram, Box Plot, Heatmap
            - Gauge, Bubble, Waterfall
            - Funnel, Radar, Tree Map
            
            ## 💻 Code Export
            - Python (Plotly, Matplotlib)
            - R (ggplot2, Plotly)
            - Image export (PNG, SVG, PDF)
